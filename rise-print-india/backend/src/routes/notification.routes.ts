import { Router } from 'express'; const router = Router(); // TODO: Implement notification routes router.get('/', (req, res) => res.json({ success: true, message: 'notification routes' })); export default router;
